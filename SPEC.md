The previous version is in the app.py with the prototype. Donot use gradio replace it with a best practice scalable coding chat assistant python application with fast api rest api for production, create the backend fast api app in a folder named backend, that connects to a vllm backend (see run-backend.sh). The frontend in the folder frontend, should be a reactjs client programmed in typescript the generated code should be ouput as html in the chat window no markdown output in the application. create a claude.md file that documents all the steps necessary to create the app. create a run backend sh vllm file that uses gpt-oss:20b instead of qwen-3.6 35b but keep the original run-backend sh file